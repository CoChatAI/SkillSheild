{
  "name": "@skillshield/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@skillshield/dashboard": "workspace:*",
    "@skillshield/shared": "workspace:*",
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "wrangler": "^4.8.0"
  }
}
